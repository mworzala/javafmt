# Files whose formatted output drops or duplicates a comment. The corpus comment-
# preservation check is skipped for these (AST-equivalence and idempotence still run).
# This is the backlog — shrink it by fixing a context and deleting the entry.

# Inline block comment inside an enum constant's argument list
# (`UNLIMITED(-1, /*Minecraft world limit*/29_999_984)`). JDT attaches it to the
# enclosing EnumConstantDeclaration, but its position-based attribution flips between
# the flat and one-per-line layouts, so routing the arguments through the comment-aware
# argument renderer is not idempotent. Left as a known drop until enum-constant
# arguments get their own stable comment handling.
mapmaker/modules/core/src/main/java/net/hollowcube/mapmaker/map/MapSize.java
