# Files whose formatted output DROPS or DUPLICATES a comment.
#
# This list MUST stay empty. The generic orphan mop-up (AstToDoc.postVisit) preserves every
# attached comment, and the STRICT emission ledger fails the build on any drop, so a real loss
# is now a located, fixable error rather than something to record here. The list — and the test
# asserting it is empty — exists only as a tripwire: if a future change reintroduces a loss, the
# fix is to emit the comment, not to add it here.
#
# (A comment that is preserved but whose coarse fallback placement normalizes once on the first
# format is NOT a loss; those few cases are listed separately as idempotence reflows in
# CorpusTest, where preservation and AST-equivalence still run.)
