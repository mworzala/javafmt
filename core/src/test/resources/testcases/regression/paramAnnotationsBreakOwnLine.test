class Example {
    void handle(@MagicConstant(flagsFromClass = Permission.class) @JsonAdapter(UnsignedLongAdapter.class) long permissions) {}
}
---
class Example {
    void handle(
        @MagicConstant(flagsFromClass = Permission.class)
        @JsonAdapter(UnsignedLongAdapter.class)
        long permissions
    ) {}
}
